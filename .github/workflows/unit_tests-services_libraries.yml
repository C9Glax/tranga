name: Libraries Service Unit Tests

on:
  push:
    branches:
      - external-connectors
    paths:
      - "Common/**"
      - "Common.Database/**"
      - "Common.Services/**"
      - "Extensions/**"
      - "GeneratedExtensionClient/**"
      - "Services.Libraries/**"
      - "Services.Libraries.Tests/**"
      - ".github/workflows/unit_tests-services_libraries.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'
      - name: Restore dependencies
        run: dotnet restore ./Services.Libraries.Tests/Services.Libraries.Tests.csproj
      - name: Build the solution
        run: dotnet build ./Services.Libraries.Tests/Services.Libraries.Tests.csproj --configuration Release --no-restore
      - name: Run unit tests with report
        run: dotnet test ./Services.Libraries.Tests/Services.Libraries.Tests.csproj --configuration Release --no-build --logger "trx;LogFileName=TestResults.trx"
      - name: Upload test results
        uses: actions/upload-artifact@v7
        with:
          name: TestResults
          path: ./Tests/TestResults/TestResults.trx